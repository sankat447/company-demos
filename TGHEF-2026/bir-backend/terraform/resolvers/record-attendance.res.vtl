## Ack{accepted}. A conditional/throttle error surfaces so the outbox retries
## (backoff); a clean write is accepted and the queued record is dropped.
#if($ctx.error)
  $util.error($ctx.error.message, $ctx.error.type)
#end
{ "accepted": true }
