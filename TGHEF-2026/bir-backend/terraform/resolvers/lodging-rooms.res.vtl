#if($ctx.error)
  $util.error($ctx.error.message, $ctx.error.type)
#end
## ROOM items already carry the full room shape (id + availability map); extra
## pk/sk keys are ignored by the LodgingRoom type.
$util.toJson($ctx.result.items)
