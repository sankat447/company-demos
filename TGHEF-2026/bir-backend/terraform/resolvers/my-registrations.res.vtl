#if($ctx.error)
  $util.error($ctx.error.message, $ctx.error.type)
#end
#set($out = [])
#foreach($it in $ctx.result.items)
  #set($row = { "id": $it.registrationId, "itemId": $it.itemId, "status": $it.status })
  #if(!$util.isNull($it.slotId)) $util.qr($row.put("slotId", $it.slotId)) #end
  #if(!$util.isNull($it.qrPassJti)) $util.qr($row.put("qrPassJti", $it.qrPassJti)) #end
  #if(!$util.isNull($it.answers)) $util.qr($row.put("answers", $it.answers)) #end
  $util.qr($out.add($row))
#end
$util.toJson($out)
