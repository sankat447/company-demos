# BACKEND_ASKS.md — contract gaps for the backend project

Rules: the app never provisions AWS resources. Anything missing from
`schemas/stack-contract.schema.json` lands here with a proposed export name.
Backend team ticks items and bumps the contract; we then update the schema in
the same PR that consumes the new key.

| #   | Status | Ask                                                                                                                                                                                                                                                                                           | Proposed export                                        | Needed by                 |
| --- | ------ | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------ | ------------------------- |
| 1   | OPEN   | IAM role for CI to publish APK/site to `appDistBucket` + invalidate the app-dist CloudFront distribution (DISTRIBUTION.md §5). Also export the distribution ID.                                                                                                                               | `ci/appDistPublishRoleArn`, `ci/appDistDistributionId` | preview.yml / release.yml |
| 2   | OPEN   | Confirm the host for `passes.jwksPath`. The app currently resolves it against the origin of `api.restBase` (src/config/stack.ts `jwksUrl()`). Prefer an absolute export.                                                                                                                      | `passes.jwksUrl` (absolute https URL)                  | P2.3 JWKS cache           |
| 3   | OPEN   | Published GraphQL SDL (or introspection allowed for a CI role) so `npm run codegen` can generate types into `src/api/generated/`. Documents currently hand-written in `src/api/graphql.ts` for: `recordScan`, `scheduleDelta`, `revocationsDelta`, `onOrderConfirmed`.                        | `api/graphqlSchemaUrl`                                 | P0/P2/P3                  |
| 4   | OPEN   | Consent registry endpoint + shape for the DPDP profile/consent screen (P1.3): list of collected fields with en+hi copy and versioning.                                                                                                                                                        | `consent.registryPath`                                 | P1.3                      |
| 5   | OPEN   | Cognito custom-auth demo number + fixed OTP for store review (Apple Sign-in review notes, DISTRIBUTION.md §7).                                                                                                                                                                                | documented secret, not a contract key                  | Phase 6                   |
| 6   | OPEN   | Flag name for attaching Play Integrity / App Attest tokens to sensitive mutations (ARCHITECTURE.md §7 security).                                                                                                                                                                              | `flags.attestation`                                    | Phase 7                   |
| 7   | OPEN   | Sentry DSN if self-hosted Sentry is offered; otherwise we stay Pinpoint-events-only. Schema already tolerates `observability.sentryDsn`.                                                                                                                                                      | `observability.sentryDsn`                              | Phase 6                   |
| 8   | OPEN   | **CO-001 E2** — REST path returning live shuttle ETAs per stop from the Location tracker, so the park-&-shuttle screen shows arrival times without device-side tracker polling.                                                                                                               | `geo.shuttleEtaPath`                                   | P6.7                      |
| 9   | OPEN   | **CO-001 E3** — SSM param for the official "Can I fly today?" status topic: the safety officer's call must fan out one weather-hold banner to every device the moment it is made.                                                                                                             | `ops.flyStatusTopicParam`                              | P3.3                      |
| 10  | OPEN   | **CO-001 E6** — REST path for the photo-based lost & found module (list/report/claim items; closure state for the 30 Nov close-out).                                                                                                                                                          | `ops.lostFoundPath`                                    | P6.8                      |
| 11  | OPEN   | **CO-001 E5** — REST path for daily cleanliness metrics (waste weighed & published) from the Cleanliness dashboard feed, rendered on the visitor clean-metrics screen.                                                                                                                        | `ops.cleanMetricsPath`                                 | P5.4                      |
| 12  | OPEN   | **CO-001 E6** — REST path for child-reunite QR wristband registration/lookup in family zones (lookup must also work offline from cache).                                                                                                                                                      | `ops.reunite.wristbandPath`                            | P6.8                      |
| 13  | OPEN   | **CO-001 E3** — flag confirming that flight bookings affected by a weather hold auto-enter the refund queue backend-side; the app only renders refund state and notifies.                                                                                                                     | `refunds.autoQueueFlag`                                | P3.3                      |
| 14  | OPEN   | Razorpay public key id for the checkout sheet. Proposed: include `providerKeyId` in the `POST /pay/order` response (typed optional in `src/payments/provider.ts`; checkout fails loudly when absent). A contract export also works.                                                           | `payments.publicKeyId` or order-response field         | P3.1                      |
| 15  | OPEN   | `getOrder(orderId)` GraphQL query returning `{orderId, status(CONFIRMED\|PENDING\|FAILED\|EXPIRED), passTokens}` — required for kill-app-between-pay-and-confirm recovery; the subscription cannot replay events fired while the app was dead. Document hand-written in `src/api/graphql.ts`. | `getOrder` in published SDL (with ask #3)              | P3.1 / Gate 5 recovery    |
