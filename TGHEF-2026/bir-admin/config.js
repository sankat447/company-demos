// Admin console config. The console talks only to the admin API (username /
// password auth, no Cognito). Public endpoint values — not secrets.
window.BIR_CONFIG = {
  restBase: 'https://8pthvvvixg.execute-api.us-east-1.amazonaws.com/v1',
  kbBucket: 'bir-2026-kb-37970b6d',
};
