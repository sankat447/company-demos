# Amboy NPI-Safe demo — single image, 4 roles. CPU-only. Pinned for reproducible
# in-cluster builds. torch is installed CPU-only from the pytorch CPU index in the
# Dockerfile (NOT here) to avoid pulling multi-GB CUDA wheels onto a GPU-less stack.

# Web / API
fastapi==0.111.0
uvicorn[standard]==0.30.1
pydantic==2.7.4
python-multipart==0.0.9          # FastAPI file uploads

# Clients for the reused platform services
httpx==0.27.0                    # Presidio + Portkey HTTP calls
psycopg[binary]==3.1.19          # Postgres + pgvector (iis-ai-postgres-primary)
boto3==1.34.140                  # MinIO S3 (raw/deid buckets)
hvac==2.3.0                      # Vault transit (HMAC tokenization)
python-jose[cryptography]==3.3.0 # verify Keycloak OIDC JWT for /detokenize

# Document text extraction for uploads (PDF / DOCX)
pypdf==4.2.0
python-docx==1.1.2
# Branded chat-transcript PDF export
reportlab==4.2.2

# Local CPU embeddings (baked model — de-identified text is NEVER sent out to vectorize)
sentence-transformers==3.0.1

# Local PII/NPI detection model (Piiranha / DeBERTa) — token-classification on CPU,
# hosted in-cluster (amboy-pii-model role). transformers comes via sentence-transformers
# but is pinned here for clarity; sentencepiece is required by the DeBERTa-v3 tokenizer.
transformers==4.41.2
sentencepiece==0.2.0

# Agent (talks to the LLM ONLY via Portkey's OpenAI-compatible endpoint).
# langchain-core is intentionally UNPINNED: langgraph + langchain-openai resolve a
# mutually-compatible 0.2.x (pinning core==0.2.11 made the build ResolutionImpossible).
langgraph==0.1.19
langchain-openai==0.1.14
openai==1.35.7

# UI
streamlit==1.36.0

# MLflow eval logging
mlflow-skinny==2.14.1
