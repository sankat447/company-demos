---
# ansible/playbooks/rollback.yml
# Triggered by AAP workflow on_failure edge OR EDA "Rollback Requested" rule.
- name: "Rollback | Uninstall patches and restore state"
  hosts: "{{ target_hosts | default('windows') }}"
  serial: 1
  gather_facts: true
  roles:
    - rollback_patch
